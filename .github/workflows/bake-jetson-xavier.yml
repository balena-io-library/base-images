name: Bake jetson-xavier
'on':
  workflow_run:
    workflows:
      - Bake aarch64
    types:
      - completed
  workflow_dispatch:
    inputs:
      no-push:
        description: Do not push to DockerHub
        required: false
        type: boolean
        default: false
      cancel-in-progress:
        description: Cancel all in-progress bake workflows and only run this one
        required: false
        type: boolean
        default: false
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: ${{ inputs.cancel-in-progress == true }}
jobs:
  prepare-jetson-xavier-alpine-golang:
    name: Prepare jetson-xavier-alpine-golang
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-alpine-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-alpine-golang:
    name: Bake jetson-xavier-alpine-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-alpine-golang
    env:
      LIBRARY: library/jetson-xavier-alpine-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-alpine-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-alpine-node:
    name: Prepare jetson-xavier-alpine-node
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-alpine-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-alpine-node:
    name: Bake jetson-xavier-alpine-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-alpine-node
    env:
      LIBRARY: library/jetson-xavier-alpine-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-alpine-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-alpine-python:
    name: Prepare jetson-xavier-alpine-python
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-alpine-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-alpine-python:
    name: Bake jetson-xavier-alpine-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-alpine-python
    env:
      LIBRARY: library/jetson-xavier-alpine-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-alpine-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-debian-golang:
    name: Prepare jetson-xavier-debian-golang
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-debian-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-debian-golang:
    name: Bake jetson-xavier-debian-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-debian-golang
    env:
      LIBRARY: library/jetson-xavier-debian-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-debian-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-debian-node:
    name: Prepare jetson-xavier-debian-node
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-debian-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-debian-node:
    name: Bake jetson-xavier-debian-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-debian-node
    env:
      LIBRARY: library/jetson-xavier-debian-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-debian-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-debian-openjdk:
    name: Prepare jetson-xavier-debian-openjdk
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-debian-openjdk.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-debian-openjdk:
    name: Bake jetson-xavier-debian-openjdk
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-debian-openjdk
    env:
      LIBRARY: library/jetson-xavier-debian-openjdk.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-debian-openjdk.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-debian-python:
    name: Prepare jetson-xavier-debian-python
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-debian-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-debian-python:
    name: Bake jetson-xavier-debian-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-debian-python
    env:
      LIBRARY: library/jetson-xavier-debian-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-debian-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-fedora-golang:
    name: Prepare jetson-xavier-fedora-golang
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-fedora-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-fedora-golang:
    name: Bake jetson-xavier-fedora-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-fedora-golang
    env:
      LIBRARY: library/jetson-xavier-fedora-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-fedora-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-fedora-node:
    name: Prepare jetson-xavier-fedora-node
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-fedora-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-fedora-node:
    name: Bake jetson-xavier-fedora-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-fedora-node
    env:
      LIBRARY: library/jetson-xavier-fedora-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-fedora-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-fedora-python:
    name: Prepare jetson-xavier-fedora-python
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-fedora-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-fedora-python:
    name: Bake jetson-xavier-fedora-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-fedora-python
    env:
      LIBRARY: library/jetson-xavier-fedora-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-fedora-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-ubuntu-golang:
    name: Prepare jetson-xavier-ubuntu-golang
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-ubuntu-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-ubuntu-golang:
    name: Bake jetson-xavier-ubuntu-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-ubuntu-golang
    env:
      LIBRARY: library/jetson-xavier-ubuntu-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-ubuntu-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-ubuntu-node:
    name: Prepare jetson-xavier-ubuntu-node
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-ubuntu-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-ubuntu-node:
    name: Bake jetson-xavier-ubuntu-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-ubuntu-node
    env:
      LIBRARY: library/jetson-xavier-ubuntu-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-ubuntu-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-ubuntu-python:
    name: Prepare jetson-xavier-ubuntu-python
    runs-on: ubuntu-latest
    needs: bake-jetson-xavier-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-ubuntu-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-ubuntu-python:
    name: Bake jetson-xavier-ubuntu-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-ubuntu-python
    env:
      LIBRARY: library/jetson-xavier-ubuntu-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-ubuntu-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-alpine:
    name: Prepare jetson-xavier-alpine
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-alpine.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-alpine:
    name: Bake jetson-xavier-alpine
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-alpine
    env:
      LIBRARY: library/jetson-xavier-alpine.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-alpine.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-debian:
    name: Prepare jetson-xavier-debian
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-debian.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-debian:
    name: Bake jetson-xavier-debian
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-debian
    env:
      LIBRARY: library/jetson-xavier-debian.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-debian.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-fedora:
    name: Prepare jetson-xavier-fedora
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-fedora.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-fedora:
    name: Bake jetson-xavier-fedora
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-fedora
    env:
      LIBRARY: library/jetson-xavier-fedora.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-fedora.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-jetson-xavier-ubuntu:
    name: Prepare jetson-xavier-ubuntu
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/jetson-xavier-ubuntu.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-jetson-xavier-ubuntu:
    name: Bake jetson-xavier-ubuntu
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-jetson-xavier-ubuntu
    env:
      LIBRARY: library/jetson-xavier-ubuntu.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-jetson-xavier-ubuntu.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
