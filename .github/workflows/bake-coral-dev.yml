name: Bake coral-dev
'on':
  workflow_run:
    workflows:
      - Bake aarch64
    types:
      - completed
  workflow_dispatch:
    inputs:
      no-push:
        description: Do not push to DockerHub
        required: false
        type: boolean
        default: false
      cancel-in-progress:
        description: Cancel all in-progress bake workflows and only run this one
        required: false
        type: boolean
        default: false
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: ${{ inputs.cancel-in-progress == true }}
jobs:
  prepare-coral-dev-alpine-golang:
    name: Prepare coral-dev-alpine-golang
    runs-on: ubuntu-latest
    needs: bake-coral-dev-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-alpine-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-alpine-golang:
    name: Bake coral-dev-alpine-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-alpine-golang
    env:
      LIBRARY: library/coral-dev-alpine-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-alpine-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-alpine-golang
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-alpine-golang-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-alpine-node:
    name: Prepare coral-dev-alpine-node
    runs-on: ubuntu-latest
    needs: bake-coral-dev-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-alpine-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-alpine-node:
    name: Bake coral-dev-alpine-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-alpine-node
    env:
      LIBRARY: library/coral-dev-alpine-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-alpine-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-alpine-node
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-alpine-node-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-alpine-python:
    name: Prepare coral-dev-alpine-python
    runs-on: ubuntu-latest
    needs: bake-coral-dev-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-alpine-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-alpine-python:
    name: Bake coral-dev-alpine-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-alpine-python
    env:
      LIBRARY: library/coral-dev-alpine-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-alpine-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-alpine-python
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-alpine-python-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-debian-golang:
    name: Prepare coral-dev-debian-golang
    runs-on: ubuntu-latest
    needs: bake-coral-dev-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-debian-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-debian-golang:
    name: Bake coral-dev-debian-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-debian-golang
    env:
      LIBRARY: library/coral-dev-debian-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-debian-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-debian-golang
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-debian-golang-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-debian-node:
    name: Prepare coral-dev-debian-node
    runs-on: ubuntu-latest
    needs: bake-coral-dev-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-debian-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-debian-node:
    name: Bake coral-dev-debian-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-debian-node
    env:
      LIBRARY: library/coral-dev-debian-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-debian-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-debian-node
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-debian-node-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-debian-openjdk:
    name: Prepare coral-dev-debian-openjdk
    runs-on: ubuntu-latest
    needs: bake-coral-dev-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-debian-openjdk.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-debian-openjdk:
    name: Bake coral-dev-debian-openjdk
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-debian-openjdk
    env:
      LIBRARY: library/coral-dev-debian-openjdk.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-debian-openjdk.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-debian-openjdk
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-debian-openjdk-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-debian-python:
    name: Prepare coral-dev-debian-python
    runs-on: ubuntu-latest
    needs: bake-coral-dev-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-debian-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-debian-python:
    name: Bake coral-dev-debian-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-debian-python
    env:
      LIBRARY: library/coral-dev-debian-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-debian-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-debian-python
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-debian-python-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-fedora-golang:
    name: Prepare coral-dev-fedora-golang
    runs-on: ubuntu-latest
    needs: bake-coral-dev-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-fedora-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-fedora-golang:
    name: Bake coral-dev-fedora-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-fedora-golang
    env:
      LIBRARY: library/coral-dev-fedora-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-fedora-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-fedora-golang
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-fedora-golang-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-fedora-node:
    name: Prepare coral-dev-fedora-node
    runs-on: ubuntu-latest
    needs: bake-coral-dev-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-fedora-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-fedora-node:
    name: Bake coral-dev-fedora-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-fedora-node
    env:
      LIBRARY: library/coral-dev-fedora-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-fedora-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-fedora-node
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-fedora-node-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-fedora-python:
    name: Prepare coral-dev-fedora-python
    runs-on: ubuntu-latest
    needs: bake-coral-dev-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-fedora-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-fedora-python:
    name: Bake coral-dev-fedora-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-fedora-python
    env:
      LIBRARY: library/coral-dev-fedora-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-fedora-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-fedora-python
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-fedora-python-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-ubuntu-golang:
    name: Prepare coral-dev-ubuntu-golang
    runs-on: ubuntu-latest
    needs: bake-coral-dev-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-ubuntu-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-ubuntu-golang:
    name: Bake coral-dev-ubuntu-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-ubuntu-golang
    env:
      LIBRARY: library/coral-dev-ubuntu-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-ubuntu-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-ubuntu-golang
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-ubuntu-golang-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-ubuntu-node:
    name: Prepare coral-dev-ubuntu-node
    runs-on: ubuntu-latest
    needs: bake-coral-dev-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-ubuntu-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-ubuntu-node:
    name: Bake coral-dev-ubuntu-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-ubuntu-node
    env:
      LIBRARY: library/coral-dev-ubuntu-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-ubuntu-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-ubuntu-node
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-ubuntu-node-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-ubuntu-python:
    name: Prepare coral-dev-ubuntu-python
    runs-on: ubuntu-latest
    needs: bake-coral-dev-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-ubuntu-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-ubuntu-python:
    name: Bake coral-dev-ubuntu-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-ubuntu-python
    env:
      LIBRARY: library/coral-dev-ubuntu-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-ubuntu-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-ubuntu-python
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-ubuntu-python-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-alpine:
    name: Prepare coral-dev-alpine
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-alpine.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-alpine:
    name: Bake coral-dev-alpine
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-alpine
    env:
      LIBRARY: library/coral-dev-alpine.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-alpine.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-alpine
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-alpine-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-debian:
    name: Prepare coral-dev-debian
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-debian.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-debian:
    name: Bake coral-dev-debian
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-debian
    env:
      LIBRARY: library/coral-dev-debian.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-debian.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-debian
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-debian-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-fedora:
    name: Prepare coral-dev-fedora
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-fedora.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-fedora:
    name: Bake coral-dev-fedora
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-fedora
    env:
      LIBRARY: library/coral-dev-fedora.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-fedora.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-fedora
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-fedora-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
  prepare-coral-dev-ubuntu:
    name: Prepare coral-dev-ubuntu
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/coral-dev-ubuntu.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-coral-dev-ubuntu:
    name: Bake coral-dev-ubuntu
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-coral-dev-ubuntu
    env:
      LIBRARY: library/coral-dev-ubuntu.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-coral-dev-ubuntu.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Update DockerHub Description
        if: inputs.no-push != true
        continue-on-error: true
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77
        with:
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
          repository: balenalib/coral-dev-ubuntu
          readme-filepath: ./balena-base-images/docs/dockerhub/coral-dev-ubuntu-full-description.md
          short-description: 'Deprecated: This image is no longer receiving updates.'
