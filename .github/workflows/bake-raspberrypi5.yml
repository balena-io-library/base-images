name: Bake raspberrypi5
'on':
  workflow_run:
    workflows:
      - Bake aarch64
    types:
      - completed
  workflow_dispatch:
    inputs:
      no-push:
        description: Do not push to DockerHub
        required: false
        type: boolean
        default: false
      cancel-in-progress:
        description: Cancel all in-progress bake workflows and only run this one
        required: false
        type: boolean
        default: false
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: ${{ inputs.cancel-in-progress == true }}
jobs:
  prepare-raspberrypi5-alpine-golang:
    name: Prepare raspberrypi5-alpine-golang
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-alpine-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-alpine-golang:
    name: Bake raspberrypi5-alpine-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-alpine-golang
    env:
      LIBRARY: library/raspberrypi5-alpine-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-alpine-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-alpine-node:
    name: Prepare raspberrypi5-alpine-node
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-alpine-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-alpine-node:
    name: Bake raspberrypi5-alpine-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-alpine-node
    env:
      LIBRARY: library/raspberrypi5-alpine-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-alpine-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-alpine-python:
    name: Prepare raspberrypi5-alpine-python
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-alpine
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-alpine-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-alpine-python:
    name: Bake raspberrypi5-alpine-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-alpine-python
    env:
      LIBRARY: library/raspberrypi5-alpine-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-alpine-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-debian-golang:
    name: Prepare raspberrypi5-debian-golang
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-debian-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-debian-golang:
    name: Bake raspberrypi5-debian-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-debian-golang
    env:
      LIBRARY: library/raspberrypi5-debian-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-debian-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-debian-node:
    name: Prepare raspberrypi5-debian-node
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-debian-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-debian-node:
    name: Bake raspberrypi5-debian-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-debian-node
    env:
      LIBRARY: library/raspberrypi5-debian-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-debian-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-debian-openjdk:
    name: Prepare raspberrypi5-debian-openjdk
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-debian-openjdk.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-debian-openjdk:
    name: Bake raspberrypi5-debian-openjdk
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-debian-openjdk
    env:
      LIBRARY: library/raspberrypi5-debian-openjdk.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-debian-openjdk.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-debian-python:
    name: Prepare raspberrypi5-debian-python
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-debian
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-debian-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-debian-python:
    name: Bake raspberrypi5-debian-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-debian-python
    env:
      LIBRARY: library/raspberrypi5-debian-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-debian-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-fedora-golang:
    name: Prepare raspberrypi5-fedora-golang
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-fedora-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-fedora-golang:
    name: Bake raspberrypi5-fedora-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-fedora-golang
    env:
      LIBRARY: library/raspberrypi5-fedora-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-fedora-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-fedora-node:
    name: Prepare raspberrypi5-fedora-node
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-fedora-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-fedora-node:
    name: Bake raspberrypi5-fedora-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-fedora-node
    env:
      LIBRARY: library/raspberrypi5-fedora-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-fedora-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-fedora-python:
    name: Prepare raspberrypi5-fedora-python
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-fedora
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-fedora-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-fedora-python:
    name: Bake raspberrypi5-fedora-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-fedora-python
    env:
      LIBRARY: library/raspberrypi5-fedora-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-fedora-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-ubuntu-golang:
    name: Prepare raspberrypi5-ubuntu-golang
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-ubuntu-golang.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-ubuntu-golang:
    name: Bake raspberrypi5-ubuntu-golang
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-ubuntu-golang
    env:
      LIBRARY: library/raspberrypi5-ubuntu-golang.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-ubuntu-golang.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-ubuntu-node:
    name: Prepare raspberrypi5-ubuntu-node
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-ubuntu-node.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-ubuntu-node:
    name: Bake raspberrypi5-ubuntu-node
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-ubuntu-node
    env:
      LIBRARY: library/raspberrypi5-ubuntu-node.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-ubuntu-node.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-ubuntu-python:
    name: Prepare raspberrypi5-ubuntu-python
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-ubuntu-python.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-ubuntu-python:
    name: Bake raspberrypi5-ubuntu-python
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-ubuntu-python
    env:
      LIBRARY: library/raspberrypi5-ubuntu-python.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-ubuntu-python.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-ubuntu-openjdk:
    name: Prepare raspberrypi5-ubuntu-openjdk
    runs-on: ubuntu-latest
    needs: bake-raspberrypi5-ubuntu
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-ubuntu-openjdk.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-ubuntu-openjdk:
    name: Bake raspberrypi5-ubuntu-openjdk
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-ubuntu-openjdk
    env:
      LIBRARY: library/raspberrypi5-ubuntu-openjdk.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-ubuntu-openjdk.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-alpine:
    name: Prepare raspberrypi5-alpine
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-alpine.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-alpine:
    name: Bake raspberrypi5-alpine
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-alpine
    env:
      LIBRARY: library/raspberrypi5-alpine.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-alpine.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-debian:
    name: Prepare raspberrypi5-debian
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-debian.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-debian:
    name: Bake raspberrypi5-debian
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-debian
    env:
      LIBRARY: library/raspberrypi5-debian.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-debian.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-fedora:
    name: Prepare raspberrypi5-fedora
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-fedora.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-fedora:
    name: Bake raspberrypi5-fedora
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-fedora
    env:
      LIBRARY: library/raspberrypi5-fedora.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-fedora.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
  prepare-raspberrypi5-ubuntu:
    name: Prepare raspberrypi5-ubuntu
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' || github.event.workflow_run.conclusion == 'failure') }}
    outputs:
      bake-targets: ${{ steps.bake-targets.outputs.matrix }}
    env:
      LIBRARY: library/raspberrypi5-ubuntu.json
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Set matrix
        id: bake-targets
        run: |
          set -x
          targets="$(jq -cr '.group.default.targets' $LIBRARY)"
          echo "matrix=$targets" >> $GITHUB_OUTPUT
  bake-raspberrypi5-ubuntu:
    name: Bake raspberrypi5-ubuntu
    runs-on:
      - self-hosted
      - base-images
      - ARM64
    timeout-minutes: 240
    needs: prepare-raspberrypi5-ubuntu
    env:
      LIBRARY: library/raspberrypi5-ubuntu.json
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.prepare-raspberrypi5-ubuntu.outputs.bake-targets) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b
        with:
          fetch-depth: 1
          persist-credentials: false
      - name: Setup buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb
        with:
          driver-opts: network=host
          install: true
      - name: Setup QEMU
        uses: docker/setup-qemu-action@49b3bc8e6bdd4a60e6116a5414239cba5943d3cf
        with:
          platforms: all
          image: tonistiigi/binfmt:qemu-v8.0.4-33
        if: contains(steps.setup-buildx.outputs.platforms, 'linux/arm64') == false
      - name: Login to DockerHub
        uses: docker/login-action@e92390c5fb421da1463c202d546fed0ec5c39f20
        if: inputs.no-push != true
        with:
          registry: docker.io
          username: ${{ secrets.BALENAIMAGES_USER }}
          password: ${{ secrets.BALENAIMAGES_TOKEN }}
      - name: Set the DATESTAMP variable
        run: echo "DATESTAMP=$(date +%Y%m%d)" >> "${GITHUB_ENV}"
      - name: Docker bake
        continue-on-error: false
        id: docker_bake
        uses: docker/bake-action@7a5dfed3550ca014665af2a27af8fc9d7284b9b3
        with:
          workdir: balena-base-images
          files: ${{ github.workspace }}/${{ env.LIBRARY }}
          targets: ${{ matrix.target }}
          push: ${{ inputs.no-push != true }}
          provenance: false
