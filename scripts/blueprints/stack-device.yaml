selector:
  sw.os: 1
  hw.device-type: 1
  sw.stack: 1
  sw.stack-variant: 1
  arch.sw: 1
output:
  filename: Dockerfile
  imageName: "{{this.children.hw.device-type.slug}}-{{this.children.sw.os.slug}}-{{this.children.sw.stack.slug}}"
  type: sw.docker-image.stack-device.{{this.children.sw.stack.slug}}
  slug: "{{this.children.hw.device-type.slug}}-{{this.children.sw.os.slug}}-{{this.children.sw.stack.slug}}:{{this.children.sw.stack.version}}-{{this.children.sw.os.version}}-{{this.children.sw.stack-variant.slug}}"
  path: "{{this.children.sw.stack.slug}}/{{this.children.hw.device-type.slug}}/{{this.children.sw.os.slug}}/{{this.children.sw.os.version}}/{{this.children.sw.stack.version}}/{{this.children.sw.stack-variant.slug}}"
  requires:
    - type: hw.device-type
      arch: "{{this.children.arch.sw.slug}}"
  template:
    - name: stack-device
      data: >
        # AUTOGENERATED FILE

        FROM balenalib/{{this.hw.device-type.slug}}-{{this.sw.os.slug}}:{{this.sw.os.version}}-{{this.sw.stack-variant.slug}}


        {{import partial=this.sw.stack-variant.slug combination="sw.stack+sw.os+hw.device-type"}}


        {{import partial="cmd" combination="sw.stack+sw.os+hw.device-type"}}


        {{import partial="onbuild" combination="sw.stack+sw.os+hw.device-type"}}
